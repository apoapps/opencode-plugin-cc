{
  "description": "swarm-code hooks — SessionStart context + PostToolUse session capture",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/task-type.mjs\" SessionStart",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
